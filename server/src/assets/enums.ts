export enum Roles {
    USER = 1,
    ADMIN = 2
};

export enum Token {
    ACCESS = 1,
    CONFIRM_EMAIL = 2,
    RESET_PASSWORD = 3,
    CONFIRM_IDENTITY = 4
};

export enum Mail {
    CONFIRM_EMAIL = 1,
    CONFIRM_IDENTITY = 2,
    RESET_PASSWORD = 3
};