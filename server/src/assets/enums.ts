export enum Roles {
    USER = 1,
    ADMIN = 2
};

export enum Token {
    REFRESH = 1,
    ACCESS = 2,
    CONFIRM_EMAIL = 3,
    RESET_PASSWORD = 4,
    CONFIRM_IDENTITY = 5
};

export enum Mail {
    CONFIRM_EMAIL = 1,
    CONFIRM_IDENTITY = 2,
    RESET_PASSWORD = 3
};